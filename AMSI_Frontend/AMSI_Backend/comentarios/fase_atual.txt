================================================================================
  ANSI PROJECT — FASE ATUAL E DIREÇÕES
================================================================================

DATA: 2026-04-21
REFERÊNCIAS: comentarios/projeto_auth.txt, comentarios/projeto_notificador.txt,
             comentarios/projeto_bateria_pytest.txt, comentarios/projeto_sessao_token.txt

--------------------------------------------------------------------------------
  O QUE JÁ ESTÁ FUNCIONANDO
--------------------------------------------------------------------------------

- CRUD completo: usuario, login, tipo_lancamento, cliente_fornecedor,
                 endereco, contato, lancamento
- Auth JWT completo:
  - POST /auth/token — login por email + bcrypt, retorna token + primeiro_acesso
  - POST /auth/logout — invalida token, registra data_logout
  - POST /auth/trocar-senha — troca senha, notifica por email, seta primeiro_acesso=False
  - Registro automático de sessão na tabela login a cada POST /auth/token
  - Sessão única por usuário — login invalida sessões anteriores
- Controle de sessão com token_ativo:
  - Tabela token_ativo no banco (jti, id_usuario_fk, criado_em, exp)
  - Token expirado é deletado ao ser usado — limpeza lazy
  - Expiração renovada a cada request autenticado
  - Header X-Session-Expires retornado em todo response autenticado
  - JWT_EXPIRE_MINUTES configurável via config.env
- Rotas protegidas: todas com get_current_user ou exige_admin
  - Rotas admin: GET/POST/DELETE /usuarios/, POST/PUT/DELETE /tipo_lancamento/,
                 todas de /login/, POST /{id}/resetar-senha
  - Rotas autenticadas: todas as demais
- Senha provisória no cadastro:
  - POST /usuarios/ gera senha aleatória, faz hash, envia por email, seta primeiro_acesso=True
  - POST /usuarios/{id}/resetar-senha — admin redefine senha, envia por email
  - Validação de domínio MX (dnspython) e email duplicado (409)
- config.env com todas as configurações sensíveis
  - utils/config.py carrega variáveis do .env com Path absoluto
  - Banco, JWT, email, ngrok, telegram — tudo configurável sem tocar no código
- CORS configurado
- Exposição via ngrok (local e online) — domínio e token via config.env
- Log de requests: /logs/ui com filtros, destaque de erros,
                   duração colorida, exportação JSON
- Swagger com HTTPBearer — autenticar colando o token no cadeado
- Bateria de testes: 52/52 passando
  - Testes positivos, negativos (401), sessão (logout + X-Session-Expires)
  - Banco limpo após execução — admin permanente preservado

--------------------------------------------------------------------------------
  PRÓXIMO PASSO IMEDIATO
--------------------------------------------------------------------------------

Iniciar o frontend React.

O backend está pronto para o MVP. O frontend pode começar a consumir a API.
Endpoints relevantes para o frontend documentados no Swagger: /docs

Pendências de backend que NÃO bloqueiam o frontend:
  1. Renomear ANSI → AMSI em todo o código e banco (baixa prioridade)
  2. Migração da bateria para pytest (pós MVP)
  3. Telegram (pós MVP)

--------------------------------------------------------------------------------
  REGRAS DE NEGÓCIO IMPLEMENTADAS
--------------------------------------------------------------------------------

1. SENHA PROVISÓRIA — COMPLETO
2. SESSÃO ÚNICA + TIMEOUT CONFIGURÁVEL — COMPLETO
3. NOTIFICAÇÃO POR EMAIL — COMPLETO (cadastro, reset, troca de senha)
4. GESTÃO DO GRUPO TELEGRAM — PÓS MVP

--------------------------------------------------------------------------------
  PENDÊNCIAS DE BANCO / MODEL
--------------------------------------------------------------------------------

- telegram_id já está no banco — adicionar ao model/schema quando implementar Telegram
- Renomear banco AMSI_Project → já correto no banco, corrigir referências no código

--------------------------------------------------------------------------------
  ESTADO DA BATERIA DE TESTES
--------------------------------------------------------------------------------

Atual: 52/52
Admin permanente: opedroschvartz@gmail.com, id=5 — NÃO é deletado pela bateria
Migração para pytest: ver comentarios/projeto_bateria_pytest.txt (pós MVP)

--------------------------------------------------------------------------------
  AVISOS IMPORTANTES
--------------------------------------------------------------------------------

- POST /usuarios/ exige exige_admin — para criar o primeiro admin comentar
  temporariamente o Depends(exige_admin), criar pelo Swagger, e reverter
- O admin permanente NUNCA deve ser deletado manualmente ou pela bateria
- Rotas com path parameter conflitante foram corrigidas:
  /por-clifor/{id} e /por-usuario/{id} em lancamento, endereco, contato e login
- dnspython é dependência obrigatória: pip install dnspython
- python-dotenv é dependência obrigatória: pip install python-dotenv
- UsuarioCreate não tem campo senha — gerada automaticamente pelo sistema
- UsuarioUpdate não tem campo senha — via POST /auth/trocar-senha ou /resetar-senha
- config.env NÃO deve ser commitado — adicionar ao .gitignore
- JWT_EXPIRE_MINUTES no config.env controla o timeout de inatividade

================================================================================